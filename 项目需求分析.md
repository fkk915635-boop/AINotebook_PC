# 笔记广场项目需求分析（结合现有登录、AI笔记功能\+SpringCloud架构\+Three\.js交互设计）

# 一、项目概述

本项目为SpringCloud微服务项目，基于现有SpringCloud\+SpringBoot\+MySQL架构，已实现用户登录、AI笔记生成与存储功能。本次新增左侧导航栏、笔记广场、点赞关注、用户信息修改功能，优化登录方式为手机号码\+验证码登录（无用户自动创建），引入Three\.js实现前端3D交互设计，重点优化导航栏、交互按钮等核心场景，提升UX体验，所有新增数据与现有数据关联，打造完整、高质感的笔记微服务系统。**注：现有功能及对应代码可根据新增需求灵活修改，不锁死原有实现方式**。

核心目标：复用现有功能，适配SpringCloud架构，实现笔记展示、社交互动、个人信息管理，通过Three\.js打造热点3D交互UI，提升用户体验与页面质感，确保服务调用顺畅、数据无冲突，同时保留功能及代码的可修改性。

# 二、现有功能梳理

现有功能为扩展基础，具体如下，且所有现有功能及代码可按需修改：

- 用户登录：原用户名\+密码登录，用户信息存储于MySQL；本次优化为手机号码\+验证码登录，新增验证码逻辑及自动注册功能，原有登录相关代码可同步调整。

- AI笔记：已实现AI生成、手动编辑及MySQL存储，可查询个人笔记，作为广场数据来源，将作为独立微服务模块；原有笔记相关功能及代码可根据广场展示、接口调用需求灵活修改。

- 技术基础：SpringCloud微服务架构，后端SpringBoot、MyBatis\-Plus，前端具备基础渲染与接口对接能力；技术实现细节可按需调整，不锁死原有架构实现；新增Three\.js库，用于前端3D交互UI开发，贴合当前热点设计趋势。

# 三、新增与优化功能需求

## 3\.1 前端左侧导航栏（Three\.js 3D交互优化，贴合热点UI）

以当前热点3D导航设计为核心，结合Three\.js实现沉浸式交互体验，兼顾美观与实用性，打造差异化UX，具体需求如下：

- 基础布局：固定于左侧，支持响应式折叠，活跃项高亮，无刷新切换页面；整体采用3D立体布局，与页面背景形成分层视觉效果，避免平面化单调感，贴合当下极简科技风热点UI。

- Three\.js核心交互（重点优化UX）：

    - 导航项3D可视化：每个导航项（首页、笔记、我的）采用3D图标\+立体文字组合，基于Three\.js Mesh模型构建，添加PBR物理渲染材质，呈现真实光影效果，区别于传统2D图标，提升视觉质感，参考当前热门3D导航设计趋势。

    - hover交互：鼠标悬浮时，导航项实现3D微旋转（沿Y轴±5°）\+ 光影高亮（环境光\+点光联动）\+ 轻微上浮（Z轴偏移），搭配平滑过渡动画（借助GSAP动画库，与Three\.js联动），反馈清晰，提升交互手感，避免生硬切换，符合热点交互设计逻辑。

    - 选中状态：当前活跃导航项，实现3D缩放（1\.1倍）\+ 专属高光描边，添加动态光效流动效果，与非活跃项形成明显视觉区分，帮助用户快速定位当前页面，强化视觉引导。

    - 折叠动画：响应式折叠时，导航项实现3D折叠过渡（沿X轴收缩），而非传统平面滑动，搭配阴影渐变效果，提升页面高级感；折叠后仅显示3D图标，节省页面空间，适配移动端，兼顾美观与实用性。

    - 背景联动：导航栏背后添加Three\.js轻量粒子背景，粒子随鼠标移动轻微联动，不干扰操作，既丰富视觉层次，又不喧宾夺主，贴合当前“轻3D、重交互”的热点UI趋势，提升页面科技感。

- 导航项对应页面：首页（笔记广场，默认页面）；笔记（现有AI笔记页面，可修改页面适配3D导航跳转逻辑）；我的（个人中心，信息展示、修改、退出登录）。

## 3\.2 笔记广场页面

- 卡片列表布局（响应式），左侧3D导航栏、右侧广场主体，按笔记创建时间倒序展示；页面整体视觉与导航栏3D风格统一，保持设计一致性。

- 笔记卡片包含：用户头像、昵称、笔记内容、发布时间、点赞数、关注按钮；点击头像/昵称跳转个人中心；卡片hover时可添加轻微3D倾斜效果（Three\.js实现），提升交互质感，贴合热点卡片设计。

- 通过微服务接口调用笔记数据，关联用户信息，实现懒加载、滚动加载；可修改原有笔记接口适配广场数据查询需求。

## 3\.3 点赞、关注功能（Three\.js交互优化）

### 3\.3\.1 点赞功能

- 登录用户可点赞/取消点赞，按钮采用Three\.js 3D建模，替换传统2D按钮；点击时实现3D缩放（1\.2倍）\+ 光影闪烁效果，取消点赞时恢复默认状态，搭配粒子迸发特效（轻量粒子，不卡顿），反馈清晰，提升交互趣味性，参考热点3D按钮交互设计。

- 点赞数实时更新，更新时添加Three\.js数字3D渐变动画，避免传统数字跳动的生硬感；已点赞状态，按钮添加3D高光描边\+颜色渐变，视觉区分明显，强化用户操作感知。

- 点赞关系存储于MySQL，Redis缓存热点数据及点赞状态，实现去重校验，确保数据同步；可结合原有数据结构修改存储及缓存逻辑。

### 3\.3\.2 关注功能

- 关注按钮采用3D立体设计（Three\.js Mesh模型），默认状态为半透明磨砂质感，hover时实现3D微抬升\+光影高亮，点击后切换为“已关注”状态（颜色加深\+3D纹理变化），状态切换流畅，贴合当前热点交互按钮设计趋势，提升UX体验。

- 关注关系存储于MySQL，Redis缓存关注/粉丝列表，可在个人中心查看关注列表；可修改个人中心相关代码适配关注列表展示，列表项hover时可添加轻微3D交互效果。

## 3\.4 用户信息修改（个人中心入口，Three\.js交互优化）

### 3\.4\.1 头像修改

- 个人中心头像采用Three\.js 3D球形模型，替代传统2D圆形头像，添加环境光反射效果，呈现真实3D质感；点击头像时，触发3D旋转动画\+光晕特效，弹出上传弹窗，交互流畅且有记忆点，贴合热点3D头像设计。

- 支持格式（jpg/png/jpeg）、大小（≤2MB）校验及预览，预览时头像实现3D实时旋转，方便用户查看效果；请求提交至用户服务。

- 头像地址存储于MySQL，同步更新Redis缓存，广场及个人中心实时同步展示；可修改原有用户表相关代码适配头像字段新增需求。

### 3\.4\.2 昵称修改

- 昵称展示采用Three\.js 3D立体文字，hover时实现3D微旋转\+颜色渐变，点击后进入编辑状态；编辑弹窗添加3D边框\+光影效果，提交按钮采用与关注按钮一致的3D设计风格，保持交互统一性，提升页面整体质感。

- 编辑规则：限制2\-10字符（无特殊字符），请求提交至用户服务。

- 昵称更新至MySQL及Redis缓存，广场及个人中心实时同步；可修改原有用户信息展示相关代码，确保同步生效。

## 3\.5 登录功能优化（Three\.js视觉优化）

- 改为手机号码\+验证码登录，手机号码为唯一标识，取消密码登录；登录按钮采用Three\.js 3D设计，hover时3D抬升\+光影变化，点击时3D缩放\+粒子特效，提升登录页面交互质感；可修改原有登录接口、身份校验相关代码，适配新登录方式。

- 6位随机验证码，有效期5分钟，同一手机号1分钟内最多获取1次，按图片规则发送；验证码输入框添加轻微3D立体效果，输入时数字实现3D渐变显示，提升输入体验。

- 校验手机号码格式、验证码正确性及有效期；无对应用户则自动创建（默认昵称、头像）；可修改用户注册相关代码，实现自动注册逻辑。

- 登录作为独立认证服务，生成JWT令牌，用于微服务接口身份校验；可调整原有认证相关代码，适配微服务身份校验需求；登录页面背景可添加Three\.js轻量3D粒子效果，贴合整体3D设计风格，不干扰登录操作。

## 3\.6 退出登录

- 个人中心退出登录按钮，采用与其他交互按钮一致的Three\.js 3D设计风格，hover时3D微旋转\+光影高亮，点击时触发3D消散动画，增强交互反馈；点击后清除登录状态、令牌及缓存，跳转登录页，通知各微服务清除临时缓存；可修改原有退出相关逻辑代码，确保缓存及状态清除彻底。

# 四、数据关联需求

- 用户表（用户服务）：新增头像字段，用户名改为手机号码，新增默认昵称/头像字段；可修改原有用户表结构及相关代码，适配字段变更。

- 笔记表（笔记服务）：通过用户ID关联用户表，广场通过接口查询用户信息；可修改笔记表关联逻辑及接口代码，确保数据关联顺畅。

- 新增点赞表、关注表（互动服务），关联用户ID、笔记ID；新增验证码表（认证服务），存储验证码相关信息；可结合原有数据结构，修改表关联及接口代码。

- Redis分布式缓存：缓存用户信息、点赞/关注数据、验证码，确保微服务间数据共享及同步；可修改原有缓存逻辑代码，适配新增数据缓存需求。

# 五、技术栈需求（重点新增Three\.js相关）

## 5\.1 后端技术栈

- 微服务：SpringCloud（Nacos、Gateway、OpenFeign）；核心框架：SpringBoot、MyBatis\-Plus；技术实现可灵活调整，不锁死具体版本及配置。

- 数据存储：MySQL（分库/分表）、Redis（分布式缓存）；可根据现有代码及业务需求，调整存储方式及相关代码。

- 其他：SpringMVC文件上传（OSS可选）、OpenFeign、JWT、Java随机数（验证码）、Spring Security\+JWT；可修改相关技术实现代码，适配项目整体需求。

## 5\.2 前端技术栈（重点新增Three\.js，贴合热点UI设计）

- 基础：HTML5、CSS3、JavaScript、Vue\.js/React；导航：Vue Router/React Router、Element UI/Ant Design；可修改前端组件及代码，适配新增页面及3D交互需求。

- Three\.js核心（重点，用于3D交互UI开发）：
        

    - 核心库：Three\.js（实现3D场景、模型、光影、粒子效果），搭配three\-mesh\-ui库优化3D UI组件开发，提升开发效率与交互质感，贴合热点3D UI开发趋势。

    - 辅助工具：GSAP（用于Three\.js动画平滑过渡，如导航项旋转、按钮缩放、粒子动画）、Cannon\-es（可选，用于复杂3D物理交互，如按钮按压反馈）、OrbitControls（优化3D场景相机控制，确保交互流畅）。

    - 核心应用：3D导航栏（图标、文字、折叠动画）、3D交互按钮（点赞、关注、登录、退出）、3D头像、3D粒子背景、3D文字（昵称、数字），贴合当前网页3D交互热点，打造沉浸式UX体验。

    - 性能优化：采用BufferGeometry优化3D模型渲染性能，避免卡顿；根据设备性能自动降级3D效果（低端设备关闭部分光影、粒子效果），确保跨设备兼容性；使用射线投射（Raycaster）实现精准的3D元素交互检测，提升交互稳定性。

- 交互：Axios、双向绑定、防抖节流；头像上传：FileReader、FormData；登录：表单校验、倒计时组件；可调整前端交互代码，适配后端接口变更及Three\.js 3D交互需求。

# 六、非功能需求

- 兼容性：适配主流浏览器及移动端，微服务接口兼容；Three\.js效果支持跨浏览器（Chrome、Edge、Firefox），移动端自动适配3D效果（简化粒子、光影，确保流畅）；可通过修改代码优化性能及兼容性，低端设备实现3D效果降级适配，避免卡顿闪退。

- 性能：页面加载≤1\.5秒，接口响应≤500ms，Redis命中率≥90%；Three\.js 3D效果不影响页面加载速度，采用懒加载机制加载3D模型及粒子资源，优化渲染性能，确保交互流畅无卡顿。

- 数据一致性：MySQL与Redis同步，微服务间数据互通；安全性：身份校验、文件校验、防攻击；可修改相关代码，提升数据一致性及安全性。

- 可维护性：微服务划分清晰，组件化开发；Three\.js代码模块化，与原有前端代码分离，便于后续修改、扩展（如调整3D交互效果、更新热点UI风格）；可用性：服务熔断降级，数据备份。

- UX体验：Three\.js 3D交互贴合当前热点，不冗余、不干扰核心操作，重点提升导航栏、按钮等高频交互场景的手感与视觉质感，实现“轻3D、重交互”，让用户操作更具趣味性与沉浸感，同时保持操作简洁性，避免过度设计。

- 不破坏现有核心功能及数据，数据无丢失；新增数据存MySQL，Redis仅缓存；技术栈统一，不新增无关框架；允许在该前提下，修改原有功能及代码，包括Three\.js相关交互代码的灵活调整。

- 用户信息修改后实时同步；登录严格按图片规则实现，手机号码唯一；导航栏入口固定，退出登录清除所有相关缓存；可修改代码确保上述约束落地，不锁死实现方式。

- Three\.js 3D交互需贴合热点UI设计，兼顾美观与性能，避免过度设计（如复杂粒子、高负载光影），确保页面流畅运行；3D风格统一（极简科技风），与整体页面设计协调，不出现视觉断层。

# 八、总结

本次基于现有功能，适配SpringCloud微服务架构，新增导航栏、广场、互动、信息修改功能，优化登录方式，引入Three\.js打造热点3D交互UI，重点优化导航栏、交互按钮等高频场景，贴合当前“轻3D、重交互”的设计趋势，提升UX体验。通过微服务划分实现高效通信，Redis优化性能，Three\.js提升页面质感与交互趣味性，形成完整业务闭环。**核心原则：现有功能及代码可根据新增需求灵活修改，不锁死原有实现；Three\.js应用兼顾美观、性能与实用性，打造差异化、高质感的前端交互体验**，贴合微服务项目要求。

> （注：文档部分内容可能由 AI 生成）
